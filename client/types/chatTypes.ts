export interface personProp {
  name: string;
  Image?: string;
  time?: string;
  lastMessage: string;
}
export interface chatBulbProps {
  image?: string;
  name: string;
  isSender: boolean;
  time: string;
  message: string;
}
